#include <stdio.h>

int main()
{
    int a = 3;
    int b = 5;
    printf("交换前：a = %d, b = %d\n", a, b);
    a = a ^ b;
    b = a ^ b;
    a = a ^ b;
    //a^b^a = a^a^b = 0^b = b   异或满足交换律
    printf("交换后：a = %d, b = %\n", a, b);
    return 0;
}
